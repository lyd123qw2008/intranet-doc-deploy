services:
  nginx:
    image: nginx:1.25-alpine
    container_name: intra-docs-nginx
    restart: always
    ports:
      - "9776:80"
    volumes:
      - /srv/intra-docs:/srv/intra-docs:ro
      - /opt/intra-docs/nginx/intra-docs.conf:/etc/nginx/conf.d/default.conf:ro
